const title = "HI!Danmu";
const url = "http://localhost:9000";
const desktop = "";
const icp = "icp备案信息";

const config = {
    title:title,
    url:url,
    desktop:desktop,
    icp:icp,
}

export default config;